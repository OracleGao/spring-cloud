server:
  port: 28082

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:28081/eureka

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/OracleGao/spring-cloud.git
          searchPaths: config-repo